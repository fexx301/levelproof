# Private pre-window planning documents (spec §16 provenance) — never deploy.
# Defense in depth: .gitignore covers commits; this covers CLI deployments.
levelBuild*.md
newVRidea*.md
taskproof.md

# OS noise
.DS_Store

# Secrets — never deploy (gitignored for commits; this covers CLI deploys)
.env
.env.*
